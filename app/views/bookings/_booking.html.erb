<div id="<%= dom_id booking %>">
  <p class="my-5">
    <strong class="block mb-1 font-medium">Name:</strong>
    <%= booking.asignee.name %>
  </p>
  <p class="my-5">
    <strong class="block mb-1 font-medium">Is coach:</strong>
    <%= booking.asignee.is_coach %>
  </p>
  <p class="my-5">
    <strong class="block mb-1 font-medium">Starts at</strong>
    <%= booking.start_at %>
  </p>
  <% if action_name != "show" or controller_name == "locations" %>
    <%= link_to "Show this asignee", booking.asignee, class: "rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
    <%= link_to "Edit this asignee", edit_asignee_path(booking.asignee), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>
</div>
